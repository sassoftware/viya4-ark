# Internal SAS site defaults
#
# For instructions on how to use this file, see:
# http://documentation.sas.com/?softwareId=calcdc&softwareVersion=production.a&softwareContextId=sitedefault
#
# PingHost failure with bad server
config:
  application:
    sas.identities.providers.ldap.connection:
      host: server.ldap.mycom.com
      password: password123
      port: 9999
      url: ldap://${mycom.some.selected.server.connection.host}:${mycom.some.selected.server.connection.port}
      anonymousBind: 'false'
      userDN: CN=[ECM] Test User,OU=Generic and Shared Accounts,OU=Admin,DC=na,DC=MyCOM,DC=com
    sas.identities.providers.ldap.group:
      baseDN: OU=Groups,DC=xa,DC=MyCOM,DC=com
    sas.identities.providers.ldap.user:
      baseDN: DC=MyCOM,DC=com
      searchFilter: sAMAccountName={0}
    sas.identities:
      administrator: 'myid'
    sas.logon.initial.password:  password
